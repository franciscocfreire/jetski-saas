/**
 * Eventos de domínio do módulo Reservas (criada/confirmada/cancelada).
 *
 * <p>Exposto como named interface para consumo cross-module (ex.: audit).
 */
@org.springframework.modulith.NamedInterface("events")
package com.jetski.reservas.domain.event;
